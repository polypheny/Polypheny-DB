{
    "version": "1.0",
    "defaultSchema": "twissandra",
    "schemas": [
        {
            "name": "twissandra",
            "type": "custom",
            "factory": "org.polypheny.db.adapter.cassandra.CassandraSchemaFactory",
            "operand": {
                "host": "localhost",
                "port": 9142,
                "keyspace": "twissandra"
            }
        }
    ]
}
